import { PlaceholderPage } from "@/components/placeholder-page";

export default function AdminPodPage() {
  return (
    <PlaceholderPage
      title="POD config"
      description="Manage Peecho provider config and offerings."
    />
  );
}
