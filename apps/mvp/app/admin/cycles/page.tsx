import { PlaceholderPage } from "@/components/placeholder-page";

export default function AdminCyclesPage() {
  return (
    <PlaceholderPage
      title="Cycles"
      description="Manage global subscription cycles and lock dates."
    />
  );
}
